@* @model IEnumerable<Proyecto_Lab_IV.Models.Vendedor> *@

@model Proyecto_Lab_IV.ModelView.VendedorVM

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-dark">Create New</a>
</p>
<table class="table table-dark table-hover text-center">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.vendedores[0].nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vendedores[0].apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vendedores[0].nroTelf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vendedores[0].correo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vendedores[0].concesionaria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.vendedores)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.apellido)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nroTelf)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.correo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.concesionaria.nombre)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-warning"><i class="bi bi-pencil-square"></i></a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info"><i class="bi bi-ticket-detailed"></i></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger"><i class="bi bi-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
<partial name="_Paginador" for="@Model.paginador" />
